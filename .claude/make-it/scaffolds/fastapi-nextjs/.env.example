# =============================================================================
# [APP_NAME] -- Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.
#
# Placeholders (replaced during /make-it build phase):
#   [APP_NAME]          - human-readable app name
#   [APP_SLUG]          - kebab-case app name
#   [FRONTEND_PORT]     - host port for frontend (e.g., 3100)
#   [BACKEND_PORT]      - host port for backend (e.g., 8100)
#   [DB_PORT]           - host port for PostgreSQL (e.g., 5500)
#   [MOCK_OIDC_PORT]    - host port for mock-oidc (e.g., 10190)
# =============================================================================

# ---------------------------------------------------------------------------
# Container Registry Proxy -- Dockyard Gateway / ACR Cache
# ---------------------------------------------------------------------------
# Routes Docker image pulls through a corporate ACR proxy cache to bypass
# SSL-inspecting proxies (Zscaler, Netskope, GlobalProtect). Leave empty
# to pull directly from public registries.
#
# Auto-discovered by /make-it via: az acr list + az acr cache list
# Example (when Dockyard Gateway is at myacr.azurecr.io):
#   DOCKER_HUB_PREFIX=myacr.azurecr.io/docker.io/library/
#   MCR_PREFIX=myacr.azurecr.io/mcr.microsoft.com/
#   GHCR_PREFIX=myacr.azurecr.io/ghcr.io/
#
# DOCKER_HUB_PREFIX -- for python, node, postgres, redis, etc.
# MCR_PREFIX        -- for mssql, dotnet, Azure SDK images, etc.
# GHCR_PREFIX       -- for zaproxy, org-published images, etc.
DOCKER_HUB_PREFIX=
MCR_PREFIX=
GHCR_PREFIX=

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
DATABASE_URL=postgresql+asyncpg://[APP_SLUG]:[APP_SLUG]@localhost:[DB_PORT]/[APP_SLUG]

# ---------------------------------------------------------------------------
# OIDC Authentication
# ---------------------------------------------------------------------------
# For local development, these point to the mock-oidc container.
# For production, replace with your Azure AD / Entra ID values.
OIDC_ISSUER_URL=http://localhost:[MOCK_OIDC_PORT]
OIDC_EXTERNAL_URL=http://localhost:[MOCK_OIDC_PORT]
OIDC_CLIENT_ID=mock-oidc-client
OIDC_CLIENT_SECRET=mock-oidc-secret

# ---------------------------------------------------------------------------
# JWT
# ---------------------------------------------------------------------------
# Generate with: openssl rand -hex 32
JWT_SECRET=

# ---------------------------------------------------------------------------
# URLs
# ---------------------------------------------------------------------------
FRONTEND_URL=http://localhost:[FRONTEND_PORT]
BACKEND_URL=http://localhost:[BACKEND_PORT]

# ---------------------------------------------------------------------------
# Security
# ---------------------------------------------------------------------------
ENFORCE_SECRETS=false

# ---------------------------------------------------------------------------
# Activity Log
# ---------------------------------------------------------------------------
LOG_BUFFER_SIZE=10000
# Cribl Stream forwarding (optional -- leave empty to disable)
CRIBL_STREAM_URL=
CRIBL_STREAM_TOKEN=

# ---------------------------------------------------------------------------
# Additional Service URLs
# ---------------------------------------------------------------------------
# [ADDITIONAL_SERVICE_URLS]
# Uncomment and configure as needed for your integrations:
#
# JIRA_BASE_URL=http://localhost:8443
# JIRA_AUTH_TOKEN=mock-jira-token
#
# TEMPO_BASE_URL=http://localhost:8444
# TEMPO_AUTH_TOKEN=mock-tempo-token
