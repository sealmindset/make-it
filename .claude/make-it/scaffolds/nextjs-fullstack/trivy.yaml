# =============================================================================
# Trivy -- Container scanning configuration
# =============================================================================
# Generated by /make-it scaffold (nextjs-fullstack)
#
# Consumed by CI pipelines (not run locally).
# Usage: trivy image --config trivy.yaml <image-name>
# =============================================================================

severity:
  - CRITICAL
  - HIGH

ignore-unfixed: true

skip-dirs:
  - node_modules
  - .next
